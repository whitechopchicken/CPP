功能：
	左值引用  避免对象拷贝：
			函数传参、函数返回值
	右值引用 C++11新特性：
		实现移动语义、实现完美转发

区别：
	左值
		举例：变量名 、 返回左值引用的函数调用、前置自增/自减、解引用、赋值运算等
	右值
		举例：字面值、返回非引用类型的函数调用，如下  、后置自增/自减 、 算术表达式、逻辑表达式、比较表达式等
			比如：某种类的初始化  T t = CreateT()  这个函数就是一个纯右值
		*将亡值  
			c++11 新引入的，与右值引用相关(移动语义)的值类型
			将亡值用来触发移动构造 
		

	
